<Window x:Class="ArchRender.Revit.UI.RenderResultWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ArchRender Result"
        Width="980" Height="720" MinWidth="500" MinHeight="400"
        WindowStartupLocation="CenterScreen"
        Background="#f8fafc"
        FontFamily="Segoe UI" FontSize="13">

  <Window.Resources>
    <Style x:Key="ToolbarButton" TargetType="Button">
      <Setter Property="Background" Value="White"/>
      <Setter Property="Foreground" Value="#1f2937"/>
      <Setter Property="BorderBrush" Value="#d1d5db"/>
      <Setter Property="BorderThickness" Value="1"/>
      <Setter Property="Padding" Value="14,7"/>
      <Setter Property="Cursor" Value="Hand"/>
      <Setter Property="Margin" Value="0,0,8,0"/>
      <Setter Property="FontWeight" Value="SemiBold"/>
    </Style>
    <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource ToolbarButton}">
      <Setter Property="Background" Value="#2563eb"/>
      <Setter Property="Foreground" Value="White"/>
      <Setter Property="BorderBrush" Value="#2563eb"/>
    </Style>
  </Window.Resources>

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <!-- Main content area -->
    <Border Grid.Row="0" Background="#1e293b" Margin="0">
      <Grid>
        <!-- Source image backdrop during loading (dimmed) -->
        <Image x:Name="SourceBackdrop" Stretch="Uniform" Opacity="0.35"
               Visibility="Collapsed"/>

        <!-- Loading state -->
        <Grid x:Name="LoadingPanel">
          <Rectangle Fill="#1e293b" Opacity="0.3"/>
          <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <Border Background="#0f172a" CornerRadius="10" Padding="28,20"
                    BorderBrush="#334155" BorderThickness="1">
              <StackPanel>
                <ProgressBar IsIndeterminate="True"
                             Width="240" Height="6"
                             Background="#1e293b" Foreground="#60a5fa"
                             BorderThickness="0"/>
                <TextBlock x:Name="LoadingText"
                           Text="Exporting view..."
                           Foreground="#e2e8f0"
                           HorizontalAlignment="Center"
                           Margin="0,14,0,0" FontSize="14" FontWeight="SemiBold"/>
              </StackPanel>
            </Border>
          </StackPanel>
        </Grid>

        <!-- Result image -->
        <Image x:Name="ResultImage" Stretch="Uniform" Visibility="Collapsed"
               Cursor="Hand" MouseDown="ResultImage_MouseDown"/>

        <!-- Error state -->
        <Border x:Name="ErrorPanel" Visibility="Collapsed"
                Background="#7f1d1d" CornerRadius="6"
                Padding="20,16" MaxWidth="500"
                VerticalAlignment="Center" HorizontalAlignment="Center">
          <TextBlock x:Name="ErrorText"
                     Foreground="#fecaca" TextWrapping="Wrap"
                     FontSize="14"/>
        </Border>
      </Grid>
    </Border>

    <!-- Bottom toolbar -->
    <Border Grid.Row="1" Background="White" BorderBrush="#e5e7eb" BorderThickness="0,1,0,0"
            Padding="16,10">
      <Grid>
        <TextBlock x:Name="CreditsText" Foreground="#6b7280"
                   VerticalAlignment="Center" FontSize="12"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
          <Button x:Name="OpenInBrowserButton" Content="Open Full Size"
                  Style="{StaticResource ToolbarButton}"
                  Click="OpenInBrowserButton_Click" IsEnabled="False"/>
          <Button x:Name="SaveButton" Content="Save Image"
                  Style="{StaticResource PrimaryButton}"
                  Click="SaveButton_Click" IsEnabled="False"/>
          <Button x:Name="CloseButton" Content="Close"
                  Style="{StaticResource ToolbarButton}"
                  Click="CloseButton_Click"/>
        </StackPanel>
      </Grid>
    </Border>
  </Grid>
</Window>
